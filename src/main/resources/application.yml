server:
  servlet:
    context-path: /api

spring:
  datasource:
    url: jdbc:h2:mem:mydb
    username: sa
    password: password
    driverClassName: org.h2.Driver
  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.H2Dialect
  h2:
    console:
      enabled: true

jwt:
  secretKey: 323аоа928328fоа
  validityInSeconds: 86400
  authorizationHeader: Authorization